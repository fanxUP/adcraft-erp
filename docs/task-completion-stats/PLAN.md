# 工作台员工完成统计入口卡片实施计划

## 模式与范围

- 模式：STRICT。
- 类型：前端信息架构调整、统计页复用、权限路由接入。
- 不新增数据库迁移、第三方依赖或业务数据写入。

实施状态：已完成，待提交并部署。

## 执行项

### P01：工作台入口卡片（对应 A01）

状态：已完成。

- 删除工作台展开的员工统计列表和空的两列布局。
- 增加管理员/普通员工差异化的紧凑卡片，卡片摘要跟随当月/全部切换。
- 整卡支持鼠标、键盘和辅助技术访问，点击跳转统计页。

### P02：独立统计页与路由（对应 A02）

状态：已完成。

- 新增 `TaskCompletionStats.vue`。
- 增加 `/reports/task-completion` 路由，使用 `task_completion:read` 页面能力。
- 从查询参数恢复统计范围，提供返回工作台入口。

### P03：统计明细交互（对应 A03）

状态：已完成。

- 展示本人或组织 KPI。
- 管理员展示员工统计表，并支持员工筛选。
- 支持按项目/按明细、阶段筛选、分页、加载/空/错误状态。

### P04：API、类型与权限契约（对应 A04）

状态：已完成。

- 在前端 API 层补充现有明细接口封装和响应类型。
- 保持后端 employee_id 越权校验、阶段可见性和价格字段白名单。
- 增加路由、工作台入口、API 参数和权限隔离回归检查。

### P05：质量验证与发布（对应 A05）

状态：验证已完成，待发布。

- 执行前端测试、类型检查、Lint、生产构建、差异范围和敏感信息检查。
- 确认 Git 工作树干净后提交并推送 `master`。
- 按现有 `deploy.sh` 发布，核对 revision、服务、健康接口和旧展开列表字符串不再出现在构建产物中。
