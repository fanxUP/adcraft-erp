# 角色权限与价格可见性验收标准

本文件与 `PLAN.md` 的 P01-P08 一一对应。所有“不可见”均以服务端响应中不出现敏感字段为准，不能以前端隐藏、返回 `null`、返回 `0` 或返回掩码代替。

## 本轮执行记录（2026-09-10）

- P01-P08 已完成代码实现和发布；线上基础烟囱已通过。六个业务测试账号的登录态逐角色验证未执行，保留为后续人工验收项。
- 后端全量回归：`1055 passed, 2 warnings`；警告为项目依赖的既有弃用提示，不是本次改造新增业务错误。
- 前端回归：25 个测试文件、153 项测试通过；Lint、类型检查、生产构建通过，首屏资源为 `152.22 KiB gzip / 200 KiB`。
- 后端重点权限/序列化文件已通过 `py_compile`；`git diff --check` 通过。
- 变更范围统计：65 个提交文件，未新增依赖，未新增数据库迁移或部署文件；未修改订单、订单明细、任务进度、成本和历史价格数据。
- 凭据扫描未发现密码字面量，变更 diff 未出现服务器地址或密码；现有无关历史文档中的服务器地址不属于本次新增内容。
- 由于当前工作区没有 `/Users/fan/.agents/skills/scope-control/engine/vibe.py`，范围引擎命令无法执行；已改用 `.vibe/SCOPE_LOCK.yaml`、变更统计和 `git diff --check` 做人工范围核对。

### 线上发布记录（2026-09-10）

- 发布版本：`37838cd7b863cd7e68d55a23397484e505ae23ac`；发布前线上版本为 `edb9e2aabe952ae87fa1385ac9957823d184eae0`。
- 数据库版本：Alembic 当前为 `u4v5w6x7y8z9 (head)`，与代码中的最新迁移头一致。
- 发布方式：使用已校验 Git bundle 和本地已通过构建的前端 dist 包；服务器发布脚本再次校验完整提交号。
- 数据保护：发布前数据库备份 `backup_20260910_135519.tar.gz` 已生成并通过备份校验；未执行业务数据清理或批量改写。
- 权限同步：新增 5 个权限已幂等同步；内置角色权限数量为 admin 120、sales 50、designer 10、production 24、installer 10、finance 28；设计/制作/安装角色敏感权限行数为 0。
- 线上检查：后端和 Nginx active；直连及代理健康接口均返回数据库正常；未登录订单接口返回 401；发布后 10 分钟后端错误日志为 0；部署标记与发布提交一致。
- 未执行项：未使用业务测试账号完成设计、制作、安装、销售、财务、管理员六类登录态逐角色接口烟囱，因此 A08 中该项仍不能标记为完成。

## A01：现状清单与敏感字段基线

- [ ] 已登记基线中的 115 个权限码、新增 5 个敏感权限（当前共 120 个）、六个内置角色及自定义角色行为。
- [ ] 已列出订单、订单明细、产品、材质、工艺、成本、收款、报表、聊天、通知、AI、导出和附件下载的接口与前端入口。
- [ ] 已形成敏感字段清单和执行角色基础字段 allowlist。
- [ ] 已标记仅校验登录、仅按角色名、或未接入统一权限策略的路由。

对应计划：P01。

## A02：敏感权限和访问策略

- [ ] 已建立并幂等初始化 `order:view_price`、`order_item:view_price`、`catalog:view_price`、`finance:view_cost`、`report:view_financial`。
- [ ] 后端统一通过访问上下文/权限能力判断价格可见性，不信任前端传入的角色或权限标记。
- [ ] 任务人员的安全订单/明细 DTO 不包含任何金额、价格、费用、成本、利润、欠款字段。
- [ ] 缺少敏感权限时，字段完全省略，而不是返回 `0`、`null`、空字符串或打码值。
- [ ] 数据库中的价格仍可用于业务计算，但不会因计算过程自动进入执行角色的响应。

对应计划：P02。

## A03：预置角色和角色管理

- [ ] 初始化脚本只同步六个内置角色，不会清空或重置自定义角色权限。
- [ ] 角色权限更新会校验全部权限 ID；任一 ID 不存在时整次操作失败并明确提示。
- [ ] 内置角色名称、核心权限和最后一个管理员保护规则已生效。
- [ ] 后端拒绝向设计、制作、安装角色授予订单价格、明细价格、目录价格和财务敏感权限。
- [ ] 后端拒绝普通账号同时拥有执行角色与销售/财务敏感角色；管理员例外。
- [ ] 角色和敏感权限变更均有操作人、时间、目标角色、变更前后值和结果日志。

对应计划：P03。

## A04：后端安全响应和模块动作权限

- [ ] 设计、制作、安装任务列表、详情、统一队列、任务处理订单明细选项均不返回订单总额、单价、费用、小计或其他价格参数。
- [ ] 设计、制作、安装任务仍能正常读取和更新其被授权的基础业务字段、状态、进度、分配人、附件和现场资料。
- [ ] 销售、财务、管理员在具备对应显式权限时，可正常读取需要的订单和明细价格。
- [ ] 订单、订单明细、产品、材质、工艺、报表、客户财务、成本、付款、导出和附件接口已按模块/动作权限保护。
- [ ] 执行角色提交伪造金额字段、价格字段或财务字段时，服务端拒绝或忽略，不能写入价格事实。
- [ ] 产品、材质、工艺对执行角色仅返回业务处理所需的基础信息，价格编辑和定价规则仅对授权角色开放。

对应计划：P04。

## A05：替代数据通道

- [ ] 工作台对设计、制作、安装角色不返回订单金额、收款、欠款、利润或财务统计字段。
- [ ] 经营报表、客户欠款、合同金额、外协/成本摘要对无财务权限用户不可读取或不返回敏感字段。
- [ ] 聊天搜索、订单/报价业务卡片、通知、最近业务对象和 AI 上下文不会向执行角色带出金额字段。
- [ ] 报价 PDF、订单报价附件、含价格导出和下载接口需要价格/财务权限；无权限时拒绝下载或返回不含价格的安全版本。
- [ ] 直接调用接口、旧页面地址、分享卡片和刷新 token 均不能绕过上述限制。
- [ ] 任务图片、视频、普通附件等不含价格的业务资料仍可按原有权限正常使用。

对应计划：P05。

## A06：前端权限能力和 UI

- [ ] `/auth/me` 或等价认证信息包含当前用户权限码/能力摘要，前端使用 `hasPermission` 判断能力。
- [ ] 路由、导航和按钮不再仅靠角色名称决定可见性；前端显示与后端权限契约一致。
- [ ] 设计、制作、安装任务列表、任务卡片、任务处理明细中不显示金额列、金额区、单价、费用和小计。
- [ ] 工作台的财务统计、欠款区和财务入口对无权限用户隐藏，且接口也不返回对应数据。
- [ ] 产品、材质、工艺页面对无目录价格权限用户隐藏价格列和价格编辑控件。
- [ ] 财务字段类型允许服务端省略；代码中不存在用 `|| 0`、默认金额或占位金额把不可见价格重新渲染出来的逻辑。
- [ ] 销售、财务、管理员的授权页面和必要价格展示不被误删。

对应计划：P06。

## A07：跨角色安全回归和代码纯净度

- [ ] 已为 admin、sales、finance、designer、production、installer 建立正向/反向测试矩阵。
- [ ] 任务、订单、明细、目录、工作台、聊天、导出和下载测试均断言敏感字段“缺失”，而不只断言页面文字不可见。
- [ ] 已验证直访 URL、伪造请求、缺权限 token、停用用户、多角色冲突、自定义角色和重启后权限持久性。
- [ ] 后端测试、前端单元测试、类型检查、Lint、生产构建全部通过；若项目已有完整测试命令，必须执行完整命令。
- [ ] 静态检查确认旧的金额渲染、未使用权限常量、重复角色判断和死代码已清理或有明确保留理由。
- [ ] 安全检查覆盖秘密、注入、路径穿越、文件下载、日志泄露和新增依赖风险。
- [ ] 验收过程中没有修改订单、订单明细、任务进度、成本和历史价格数据。

对应计划：P07。

## A08：发布、线上烟囱和回滚

- [x] 发布前已记录当前提交、数据库版本、服务状态和回滚点。
- [x] 已备份数据库及权限关联数据；权限 seed 可重复执行且不会覆盖自定义角色。
- [x] 发布后健康检查、权限拒绝路径和前端静态资源均正常；业务账号登录态验证仍待补。
- [ ] 使用隔离测试账号验证六类角色：设计/制作/安装无价格，销售/财务/管理员按权限可见。
- [ ] 已保留发布前应用版本和回滚策略；未实际执行回滚演练，回滚不会删除业务数据或覆盖无关表。
- [x] 已记录发布版本、验证结果、异常和回滚决策。

对应计划：P08。
